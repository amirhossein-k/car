import { createSlice } from "@reduxjs/toolkit";

const Rowcarslice = createSlice({
      name:"car",
      initialState: {
            carlist:[]
      },
      reducers:{}
})

export default Rowcarslice.reducer